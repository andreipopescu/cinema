from setuptools import setup

setup(
    name='cinema',
    version='1.0.0',
    packages=['cinema'],
    include_package_data=True,
    install_requires=[
        'Flask==0.12.2',
        
    ],
)